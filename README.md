markdown
# MiniCompiler – Компилятор C-подобного языка

[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)

## Содержание

- [Описание проекта](#описание-проекта)
- [Структура проекта](#структура-проекта)
- [Лексический анализ](#лексический-анализ)
  - [Нововведения и изменения](#нововведения-и-изменения)
  - [Спецификация языка](#спецификация-языка)
  - [Функциональность](#функциональность)
- [Синтаксический анализ](#синтаксический-анализ)
  - [Нововведения и изменения](#нововведения-и-изменения)
  - [Формальная грамматика](#формальная-грамматика)
  - [Форматы вывода AST](#форматы-вывода-ast)
- [Семантический анализ](#семантический-анализ)
  - [Нововведения и изменения](#нововведения-и-изменения)
  - [Система типов](#система-типов)
  - [Правила типизации](#правила-типизации)
  - [Формат сообщений об ошибках](#формат-сообщений-об-ошибках)
- [Установка и сборка](#установка-и-сборка)
  - [Требования](#требования)
  - [Сборка проекта](#сборка-проекта)
  - [Проверка установки](#проверка-установки)
- [Использование](#использование)
  - [Запуск лексера](#запуск-лексера)
  - [Запуск парсера](#запуск-парсера)
  - [Формат вывода](#формат-вывода)
  - [Примеры](#примеры)
- [Тестирование](#тестирование)
  - [Запуск тестов](#запуск-тестов)
  - [Структура тестов](#структура-тестов)
  - [Добавление новых тестов](#добавление-новых-тестов)


---

## Описание проекта

**MiniCompiler** — это учебный компилятор для упрощённого C-подобного языка, разрабатываемый итеративно с использованием методологии спринтов. Проект создан с целью изучения принципов построения компиляторов: от лексического анализа до генерации кода.

Каждый спринт добавляет новый этап компиляции, строго следуя техническим требованиям и поддерживая чистую архитектуру кода.

**Текущий спринт:** Спринт 1 – Лексический анализатор (лексер).

---


## Структура проекта

Проект организован с учётом модульности и лёгкой расширяемости для будущих спринтов:
```
compiler-project/
├── src/ # Исходный код компилятора
│ ├── lexer/ # Модуль лексического анализа
│ │ ├── init.py
│ │ ├── token.py # Определения токенов (TokenType, Token)
│ │ ├── scanner.py # Сканер (основная логика)
│ │ ├── error.py # Классы ошибок лексического анализа
│ │ └── cli.py # Интерфейс командной строки
│ ├── parser/ # Модуль синтаксического анализа
│ │ ├── init.py
│ │ ├── parser.py # Рекурсивный нисходящий парсер
│ │ ├── ast.py # Иерархия узлов AST
│ │ ├── grammar.txt # Формальная грамматика в EBNF
│ │ ├── visitor.py # Базовый класс Visitor
│ │ ├── pretty_printer.py # Красивый вывод AST в текстовом формате
│ │ ├── dot_generator.py # Генерация DOT
│ │ └── cli.py # CLI для парсера
│ ├── semantic/            # Модуль семантического анализа (Спринт 3)
│ │   ├── __init__.py
│ │   ├── analyzer.py      # Семантический анализатор (Visitor)
│ │   ├── symbol_table.py  # Иерархическая таблица символов
│ │   ├── type_system.py   # Система типов
│ │   ├── errors.py        # Классы ошибок семантического анализа
│ │   └── cli.py          # CLI для семантического анализатора
│ └── utils/ # Вспомогательные утилиты
│ ├── init.py
│ └── file_reader.py # Чтение файлов с поддержкой разных окончаний строк
├── tests/ # Тесты
│ ├── lexer/ # Тесты лексера 
│ │ ├── valid/
│ │ └── invalid/
│ ├── parser/ # Тесты парсера 
│ │ ├── valid/
│ │ │ ├── expressions/
│ │ │ ├── statements/
│ │ │ ├── declarations/
│ │ │ └── full_programs/
│ │ └── invalid/
│ │ ├── syntax_errors/
│ │ └── semantic_errors/
│ └── test_runner.py # Скрипт для автоматического запуска всех тестов
├── examples/ # Примеры исходного кода на языке MiniCompiler
│ ├── hello.src
│ ├── factorial.src
│ └── struct_example.src
├── docs/ # Документация
│ ├── language_spec.md # Спецификация лексической грамматики 
│ └── grammar.md # Формальная грамматика 
├── README.md # Данный файл
├── pyproject.toml # Система сборки (setuptools)
└── .gitignore # Игнорируемые файлы Git

text
```
---

## Лексический анализ

### Нововведения и изменения

В результате выполнения Спринта 1 в проект были внесены следующие изменения:

1.  **Создана базовая структура проекта:** Организована модульная архитектура с разделением на `src/`, `tests/`, `examples/`, `docs/`.
2.  **Реализована система сборки:** Добавлен `pyproject.toml` для установки пакета и создания CLI-команды `compiler`.
3.  **Разработана спецификация языка:** В `docs/language_spec.md` формально описана лексическая грамматика (токены, ключевые слова, литералы, операторы, комментарии) с использованием EBNF.
4.  **Написан лексический анализатор (сканер):**
    - Преобразует исходный код в последовательность токенов.
    - Отслеживает позицию каждого токена (строка, колонка).
    - Поддерживает все типы токенов из спецификации.
    - Корректно обрабатывает однострочные (`//`) и многострочные (`/* ... */`) комментарии.
    - Распознаёт многосимвольные операторы (`==`, `!=`, `<=`, `>=`, `&&`, `||`, `+=` и др.).
    - Извлекает значения литералов (целые числа, числа с плавающей точкой, строки).
5.  **Добавлена система обработки ошибок:**
    - Обнаруживает недопустимые символы, незакрытые строки и комментарии.
    - Выдаёт понятные сообщения об ошибках с указанием строки и колонки.
    - Реализовано восстановление после ошибок (сканирование продолжается).
6.  **Создан набор тестов:**
    - Автоматизированный раннер тестов (`test_runner.py`).
    - Более 20 тестов на валидные конструкции и 10+ тестов на ошибочные ситуации.
    - Тесты сравнивают фактический вывод лексера с эталонными файлами (`.expected`).
7.  **Добавлен интерфейс командной строки:** Команда `compiler lex` для запуска лексера на файле.

### Спецификация языка

Полная спецификация лексической грамматики находится в файле [`docs/language_spec.md`](docs/language_spec.md). Краткое описание:

- **Ключевые слова:** `if`, `else`, `while`, `for`, `int`, `float`, `bool`, `return`, `true`, `false`, `void`, `struct`, `fn`
- **Идентификаторы:** Начинаются с буквы или `_`, далее буквы, цифры или `_`. Длина до 255 символов.
- **Литералы:** целые (`123`), вещественные (`123.456`), строковые (`"hello"`), булевы (`true`/`false`).
- **Операторы:** арифметические (`+`, `-`, `*`, `/`, `%`), сравнения (`==`, `!=`, `<`, `<=`, `>`, `>=`), логические (`&&`, `||`, `!`), присваивания (`=`, `+=`, `-=`, `*=`, `/=`, `%=`).
- **Разделители:** `( ) { } [ ] ; , .`
- **Комментарии:** однострочные (`// ...`) и многострочные (`/* ... */`).
- **Пробельные символы:** пробел, табуляция, перевод строки (`\n`), возврат каретки (`\r`) — игнорируются.

### Функциональность

Текущая версия программы (`compiler lex`) выполняет следующие функции:

- Читает исходный файл с кодировкой UTF-8.
- Разбивает текст на токены в соответствии с лексической грамматикой.
- Для каждого токена выводит: строку, колонку, тип токена, лексему и (для литералов) значение.
- При обнаружении ошибок выводит диагностические сообщения в `stderr` и завершается с ненулевым кодом возврата.

---

## Установка и сборка

### Требования

- Python версии 3.9 или выше.
- `pip` (устанавливается вместе с Python).

### Сборка проекта

Проект использует стандартный инструментарий Python. Для "сборки" (установки пакета в режиме разработки) выполните в корневой директории проекта:

```bash

# 1. Клонирование репозитория

git clone https://github.com/IvanMorst/compiler-project

cd compiler-project
```
# 2. Установите пакет в режиме разработки (editable mode)
pip install -e .
Эта команда установит пакет minicompiler и создаст глобально доступную команду compiler. Все изменения в исходном коде будут сразу доступны без переустановки.

python -m venv venv    

venv\Scripts\activate  

### Проверка установки
После установки проверьте, что команда compiler доступна:

```
bash
compiler --help
```
Вы должны увидеть справку по доступным командам.

Использование
## Запуск лексера
Для запуска лексического анализатора на файле с исходным кодом используйте команду:

```

bash

compiler lex --input <путь_к_входному_файлу> --output <путь_к_выходному_файлу>
Параметры:

--input, -i — обязательный параметр, путь к файлу с исходным кодом (например, examples/hello.src).

--output, -o — обязательный параметр, путь к файлу, куда будут записаны токены (например, tokens.txt).
```
Пример:

```

bash

 python -m src.lexer.cli lex --input examples/hello.src --output tokens.txt             

```

Формат вывода
Выходной файл содержит по одному токену на строку в следующем формате:

````
<СТРОКА>:<КОЛОНКА> <ТИП_ТОКЕНА> "<ЛЕКСЕМА>" [<ЗНАЧЕНИЕ_ЛИТЕРАЛА>]
<СТРОКА> и <КОЛОНКА> — позиция начала токена (индексация с 1).

<ТИП_ТОКЕНА> — имя элемента перечисления TokenType (например, KW_IF, IDENTIFIER, INT_LITERAL).

<ЛЕКСЕМА> — исходный текст токена, заключённый в двойные кавычки.

[<ЗНАЧЕНИЕ_ЛИТЕРАЛА>] — необязательное поле. Присутствует только для токенов-литералов (INT_LITERAL, FLOAT_LITERAL, STRING_LITERAL) и содержит преобразованное значение (число или строку без кавычек).
````
Пример строки вывода:
````
text
2:18 INT_LITERAL "42" 42
В конце файла всегда присутствует токен END_OF_FILE:

text
5:1 END_OF_FILE ""
Примеры
Входной файл examples/hello.src:
text
fn main() {
    int counter = 42;
    return counter;
}
````

Команда:

````
bash
compiler lex -i examples/hello.src -o tokens.txt
````

Содержимое tokens.txt:

````
text
1:1 KW_FN "fn"
1:4 IDENTIFIER "main"
1:8 LPAREN "("
1:9 RPAREN ")"
1:10 LBRACE "{"
2:5 KW_INT "int"
2:9 IDENTIFIER "counter"
2:16 ASSIGN "="
2:18 INT_LITERAL "42" 42
2:19 SEMICOLON ";"
3:5 KW_RETURN "return"
3:12 IDENTIFIER "counter"
3:19 SEMICOLON ";"
4:1 RBRACE "}"
5:1 END_OF_FILE ""
````
### Запуск парсера

#### Для запуска синтаксического анализатора:

```bash
compiler parse --input <путь_к_входному_файлу> --output <путь_к_выходному_файлу> [--format {text|dot|json}] [--verbose]
Параметры:

--input, -i — путь к файлу с исходным кодом

--output, -o — путь к выходному файлу

--format, -f — формат вывода AST (по умолчанию: text)

--verbose, -v — подробный вывод (включая ошибки парсинга)
````
Примеры:

```bash
# Текстовый формат
compiler parse -i examples/factorial.src -o ast.txt

# Graphviz DOT
compiler parse -i examples/factorial.src -f dot -o ast.dot

# JSON
compiler parse -i examples/factorial.src -f json -o ast.json

# Подробный вывод
compiler parse -i examples/factorial.src -o ast.txt -v
Форматы вывода AST
Текстовый формат
Человеко-читаемое представление с отступами и аннотациями строк.


Для визуализации графа AST. Преобразование в PNG:
````
```bash
dot -Tpng ast.dot -o ast.png
````

## Синтаксический анализ 
### Нововведения и изменения
````
В Спринте 2 реализован синтаксический анализатор, который строит абстрактное синтаксическое дерево (AST) из потока токенов:

Рекурсивный нисходящий парсер (LL(1)):

Один токен предпросмотра

Методы: match(), consume(), check(), peek()

Обработка всех конструкций языка согласно грамматике

Полная иерархия узлов AST:

Выражения: литералы, идентификаторы, бинарные/унарные операции, вызовы функций, присваивания

Инструкции: блоки, if/else, while, for, return, объявления переменных

Объявления: функции, структуры

Обработка приоритетов операторов (9 уровней):

Assignment → LogicalOr → LogicalAnd → Equality → Relational → Additive → Multiplicative → Unary → Primary

Корректная ассоциативность (правая для присваивания, левая для остальных)

Восстановление после ошибок:

Метод synchronize() для пропуска до безопасной точки

Продолжение парсинга после обнаружения ошибки

Сбор всех ошибок перед завершением

Три формата вывода AST:

Text: человеко-читаемое дерево с отступами

DOT: граф для визуализации через Graphviz

JSON: структурированное представление для автоматической обработки
````
### Формальная грамматика
#### Полная грамматика в нотации EBNF находится в docs/grammar.md. Основные правила:
````
text
Program        ::= { Declaration } EOF
Declaration    ::= FunctionDecl | StructDecl | VarDecl
FunctionDecl   ::= "fn" IDENTIFIER "(" [ Parameters ] ")" [ "->" Type ] Block
StructDecl     ::= "struct" IDENTIFIER "{" { VarDecl } "}"
Block          ::= "{" { Statement } "}"
Statement      ::= Block | IfStmt | WhileStmt | ForStmt | ReturnStmt | ExprStmt | VarDecl | ";"
Expression     ::= Assignment
Assignment     ::= LogicalOr { ("=" | "+=" | "-=" | "*=" | "/=" | "%=") Assignment }

````
Запуск парсера
```bash
# Текстовый формат (по умолчанию)
compiler parse -i program.src -o ast.txt

# DOT формат для визуализации
compiler parse -i program.src -f dot -o ast.dot
dot -Tpng ast.dot -o ast.png

# JSON формат
compiler parse -i program.src -f json -o ast.json

# Подробный вывод с ошибками
compiler parse -i program.src -o ast.txt -v
````
## Пример вывода
### Для кода:
````
c
fn add(int a, int b) -> int {
    return a + b;
}
````
### Текстовый формат AST:

````
Program [line 1]:
  FunctionDecl: add -> int [line 1]:
    Parameters: [int a, int b]
    Body:
      Block [line 1-3]:
        Return: (a + b) [line 2]
````
## Семантический анализ

### Нововведения и изменения

В результате выполнения Спринта 3 в проект были внесены следующие изменения:

1. **Реализована иерархическая таблица символов:**
   - Поддержка вложенных областей видимости (глобальная, функции, блоки)
   - Поиск символов с учётом правил видимости (от внутренней к внешней)
   - Хранение полной информации о символах (тип, вид, позиция, параметры)

2. **Разработана система типов:**
   - Базовые типы: int, float, bool, void, string
   - Составные типы: struct, function
   - Проверка совместимости типов (с учётом расширения int → float)
   - Типовые аннотации для всех узлов выражений

3. **Создан семантический анализатор:**
   - Проверка объявлений (дубликаты, видимость)
   - Контроль типов в выражениях и присваиваниях
   - Валидация вызовов функций (количество и типы аргументов)
   - Проверка возвращаемых значений
   - Анализ управляющих конструкций (условия должны быть boolean)

4. **Реализована обработка семантических ошибок:**
   - Понятные сообщения с указанием позиции
   - Категории ошибок: необъявленные переменные, несоответствие типов, ошибки вызова функций
   - Продолжение анализа после обнаружения ошибок

5. **Добавлен декорированный AST:**
   - Каждый узел выражения содержит выведенный тип
   - Идентификаторы связаны с записями в таблице символов
   - Сохранение результатов разрешения перегрузок

6. **Созданы тесты семантического анализатора:**
   - Тесты на корректные программы (5+ тестов)
   - Тесты на ошибочные ситуации (7+ тестов)
   - Проверка всех категорий семантических ошибок

### Правила типизации

#### Совместимость типов:
- `int` совместим с `int`
- `float` совместим с `float` и `int` (автоматическое расширение)
- `bool` совместим с `bool`
- `void` - специальный тип для функций без возвращаемого значения

#### Бинарные операции:
| Оператор | Типы операндов | Тип результата |
|----------|---------------|-----------------|
| +, -, *, /, % | int, int | int |
| +, -, *, / | float, float | float |
| +, -, *, / | int, float или float, int | float |
| ==, !=, <, <=, >, >= | int/float, int/float | bool |
| &&, \|\| | bool, bool | bool |

#### Унарные операции:
| Оператор | Тип операнда | Тип результата |
|----------|-------------|-----------------|
| - | int | int |
| - | float | float |
| ! | bool | bool |

### Формат сообщений об ошибках
semantic error: type mismatch in assignment
--> program.src:15:10
expected: int
found: string

semantic error: undeclared variable 'unknown'
--> program.src:22:5
variable 'unknown' not found in this scope

semantic error: argument count mismatch
--> program.src:30:5
function 'bar' expects 1 argument, got 2

text

## Запуск семантического анализатора

```bash
# Базовый запуск
compiler check --input <путь_к_файлу> [--output <выходной_файл>]
````
# Подробный вывод с таблицей символов
```bash
compiler check -i program.src --verbose --dump-symbols
```
# Показать типы выражений
```bash
compiler check -i program.src --show-types
Параметры команды check
--input, -i — обязательный параметр, путь к файлу с исходным кодом

--output, -o — путь к файлу для отчёта о проверке

--verbose, -v — подробный вывод процесса анализа

--show-types — показать типы всех выражений в AST

--dump-symbols — вывести содержимое таблицы символов

```
Запуск тестов семантического анализатора
bash
# Запуск только семантических тестов
```bash
pytest tests/semantic/test_semantic.py -v
````
# Запуск всех тестов проекта

````bash
pytest tests/ -v
text
````
### 4. Обновить раздел "Использование"

Добавить в начало раздела:


### Доступные команды
```
- `compiler lex` — лексический анализ (Спринт 1)
- `compiler parse` — синтаксический анализ с построением AST (Спринт 2)
- `compiler check` — семантический анализ с проверкой типов (Спринт 3)
- `compiler test` — запуск тестов
```
### Тестирование
````
Качество кода и корректность работы лексера обеспечиваются автоматизированными тестами.

# Установка зависимостей для тестирования
pip install -e ".[test]"

# Запуск всех тестов
pytest -v tests/

# Запуск с подробным выводом
pytest -v

````
#### Запуск тестов
Для запуска всех тестов выполните в корневой директории проекта:

```
bash

python tests/test_runner.py

```


#### Структура тестов


### Добавление новых тестов

Создайте файл с исходным кодом в соответствующей директории (valid или invalid) с расширением .src. Имя файла должно отражать суть теста (например, test_long_identifier.src).

Запустите лексер на этом файле вручную и убедитесь, что вывод корректен.

Сохраните этот вывод в файл с тем же именем, но с расширением .expected в той же директории.

При следующем запуске test_runner.py новый тест будет автоматически обнаружен и выполнен.



